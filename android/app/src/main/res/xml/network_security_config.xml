<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!--
      Phase 9.6: Android Network Security Configuration.
      Enforces HTTPS by default for all production traffic.
      Scopes cleartext HTTP strictly to local development host/LAN IP endpoints.
    -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- Android Emulator loopback host -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <!-- Localhost loopback -->
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Development LAN subnet endpoints -->
        <domain includeSubdomains="true">192.168.1.100</domain>
        <domain includeSubdomains="true">192.168.0.1</domain>
        <domain includeSubdomains="true">192.168.1.1</domain>
        <domain includeSubdomains="true">192.168.29.1</domain>
    </domain-config>
</network-security-config>
